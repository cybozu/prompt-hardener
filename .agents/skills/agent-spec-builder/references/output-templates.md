# Output Templates

Templates for the 3 files generated by `agent-spec-builder`.

Only schema-supported fields belong in `agent_spec.yaml`. Extra evidence or caveats belong in `evidence.md` or `open_questions.md`.

---

## 1. agent_spec.yaml

### Field Ordering

```yaml
version: "1.0"
type: <type>
name: "<name>"
description: "<description>"

system_prompt: |
  <system prompt text>

# messages: (optional)
#   - role: user
#     content: "..."
#   - role: assistant
#     content: "..."

provider:
  api: <api>
  model: <model>
  # region: <region>    # bedrock only
  # profile: <profile>  # bedrock only

# tools: (agent, mcp-agent)
# policies: (optional, recommended when tools or boundaries exist)
# data_sources: (required for rag, optional otherwise)
# mcp_servers: (mcp-agent)

user_input_description: "<description>"
has_persistent_memory: "<true|false|unknown>"
scope: "<single_user|shared_workspace|multi_tenant|unknown>"
```

### Tool Entry Template

```yaml
tools:
  - name: <tool_name>
    description: "<what this tool does>"
    parameters:
      type: object
      properties:
        <param_name>:
          type: <param_type>
          description: "<param description>"
      required: [<required_params>]
    effect: <read|write|delete|external_send|unknown>
    impact: <low|medium|high|unknown>
    execution_identity: <user|service|mixed|unknown>
    source: <local|third_party|mcp|unknown>
    # version: "<version>"           # include when known and relevant
    # content_hash: "sha256:..."     # include when known and relevant
```

### Data Source Template

```yaml
data_sources:
  - name: <source_name>
    type: <source_type>
    trust_level: <trusted|untrusted|unknown>
    description: "<description>"
    sensitivity: <public|internal|confidential|unknown>
```

### MCP Server Template

```yaml
mcp_servers:
  - name: <server_name>
    trust_level: <trusted|untrusted|unknown>
    allowed_tools:
      - <tool_name>
    data_access:
      - <data_source>
    source: <first_party|third_party|unknown>
    # version: "<version>"
    # content_hash: "sha256:..."
```

### Policies Template

```yaml
policies:
  allowed_actions:
    - <tool_name>
  denied_actions:
    - <action>
  data_boundaries:
    - "<rule>"
  escalation_rules:
    - condition: "<when>"
      action: "<what to do>"
  # max_tool_calls: 20
  # max_steps: 10
  # rate_limits:
  #   - "10 calls/minute"
  # cost_budget: "$1.00/session"
```

### YAML Comment Conventions

```yaml
effect: write  # confidence: high — explicit file mutation in implementation
impact: high  # confidence: medium — inferred from billing mutation path
execution_identity: unknown  # TODO: Set to user/service/mixed -> enables TOOL-004 (high)
source: unknown  # TODO: Set to local/third_party/mcp -> enables ARCH-008 (high)
trust_level: untrusted  # safe default — set to trusted only if the source is controlled
# version: "<version>"  # TODO: Needed for ARCH-008 when source is third_party or unknown
# content_hash: "sha256:..."  # TODO: Needed for ARCH-008 when source is third_party or unknown
```

### Minimal Type Skeletons

#### chatbot

```yaml
version: "1.0"
type: chatbot
name: "<name>"
description: "<description>"

system_prompt: |
  <prompt>

provider:
  api: <api>
  model: <model>

user_input_description: "<description>"
has_persistent_memory: "<value>"
scope: "<value>"
```

#### rag

```yaml
version: "1.0"
type: rag
name: "<name>"
description: "<description>"

system_prompt: |
  <prompt>

provider:
  api: <api>
  model: <model>

data_sources:
  - name: <source_name>
    type: <source_type>
    trust_level: <trusted|untrusted|unknown>
    sensitivity: <public|internal|confidential|unknown>

policies:
  data_boundaries:
    - "<rule>"

user_input_description: "<description>"
has_persistent_memory: "<value>"
scope: "<value>"
```

#### agent

```yaml
version: "1.0"
type: agent
name: "<name>"
description: "<description>"

system_prompt: |
  <prompt>

provider:
  api: <api>
  model: <model>

tools:
  - name: <tool_name>
    description: "<description>"
    effect: <effect>
    impact: <impact>
    execution_identity: <identity>
    source: <source>

policies:
  allowed_actions:
    - <tool_name>
  denied_actions:
    - <action>
  escalation_rules:
    - condition: "<when>"
      action: "<what to do>"
  # max_tool_calls: 20

user_input_description: "<description>"
has_persistent_memory: "<value>"
scope: "<value>"
```

#### mcp-agent

```yaml
version: "1.0"
type: mcp-agent
name: "<name>"
description: "<description>"

system_prompt: |
  <prompt>

provider:
  api: <api>
  model: <model>

tools:
  - name: <tool_name>
    description: "<description>"
    effect: <effect>
    impact: <impact>
    execution_identity: <identity>
    source: <source>

mcp_servers:
  - name: <server_name>
    trust_level: <trusted|untrusted|unknown>
    allowed_tools:
      - <tool_name>
    source: <first_party|third_party|unknown>

policies:
  allowed_actions:
    - <tool_name>
  denied_actions:
    - <action>
  escalation_rules:
    - condition: "<when>"
      action: "<what to do>"
  # max_tool_calls: 20

user_input_description: "<description>"
has_persistent_memory: "<value>"
scope: "<value>"
```

---

## 2. evidence.md

```markdown
# Agent Spec Builder — Evidence Log

Generated: <YYYY-MM-DD>
Mode: <from-code|from-questions>
Source: <repo path or "interactive">

## Summary

- **Type**: <type> (confidence: <high|medium|low>)
- **Fields populated**: <N> / <total>
- **High-confidence fields**: <N>
- **Fields requiring review**: <N>

## Field Evidence

| Field | Value | Confidence | Source |
|-------|-------|------------|--------|
| version | "1.0" | high | Schema constant |
| type | <value> | <confidence> | <source> |
| system_prompt | (see YAML) | <confidence> | <file:line or user response> |
| provider.api | <value> | <confidence> | <source> |
| tools[0].effect | <value> | <confidence> | <source or reasoning> |
| tools[0].source | <value> | <confidence> | <ownership or integration evidence> |
| tools[0].version | <value> | <confidence> | <manifest or config source> |
| tools[0].content_hash | <value> | <confidence> | <checksum source> |
| policies.max_tool_calls | <value> | <confidence> | <config source> |
| data_sources[0].sensitivity | <value> | <confidence> | <schema, code, or user answer> |
| mcp_servers[0].source | <value> | <confidence> | <config or ownership evidence> |
| has_persistent_memory | <value> | <confidence> | <source> |
| scope | <value> | <confidence> | <source> |

## Additional Security Evidence

### Provenance Evidence

- <tool or server name>: <why source/version/hash was chosen>

### Budget Evidence

- <where execution ceilings were found, or why they remain unresolved>

### Memory Protection Evidence

- <signals such as validation before write, expiry, rollback, provenance, segmentation>

### Tenant Isolation Evidence

- <signals such as per-tenant namespace, per-user memory, scoped retrieval>

## Inference Details

### <field_path> = <value> (confidence: <level>)

**Reasoning:** <why this value was chosen>
**Evidence:** <specific file:line or user response>
**Alternative interpretations:** <other values considered>
```

### Evidence Source Formats

- `src/agent.py:45 — @tool decorator found`
- `pyproject.toml:18 — version pin found for external tool package`
- `mcp.json:12 — remote MCP server config`
- `User stated: "Runs with a service account"`
- `Default value — could not determine from available information`

---

## 3. open_questions.md

```markdown
# Open Questions

Items below could not be determined automatically. Resolving them enables additional
security analysis rules in Prompt Hardener.

## Critical Priority

### tools[<N>].impact
- **Current value**: `unknown`
- **Why it matters**: Enables **TOOL-003** (critical) for high-impact tool escalation coverage.
- **How to resolve**: Choose `low`, `medium`, or `high` based on worst-case misuse.

### tools[<N>].effect
- **Current value**: `unknown`
- **Why it matters**: Enables **TOOL-006** (critical) and sharpens **TOOL-001** / **ARCH-002**.
- **How to resolve**: Set `read`, `write`, `delete`, or `external_send`.

## High Priority

### tools[<N>].execution_identity
- **Current value**: `unknown`
- **Why it matters**: Enables **TOOL-004** (high).
- **How to resolve**: Set `user`, `service`, or `mixed`.

### tools[<N>].source
- **Current value**: `unknown`
- **Why it matters**: Enables provenance checks for **ARCH-008** (high).
- **How to resolve**: Set `local`, `third_party`, or `mcp`.

### tools[<N>].version
- **Current value**: not defined
- **Why it matters**: Needed for **ARCH-008** when the tool is third-party.
- **How to resolve**: Provide the pinned tool or package version.

### tools[<N>].content_hash
- **Current value**: not defined
- **Why it matters**: Needed for **ARCH-008** when the tool is third-party.
- **How to resolve**: Provide a trusted checksum or digest.

### mcp_servers[<N>].source
- **Current value**: `unknown`
- **Why it matters**: Enables **ARCH-008** (high).
- **How to resolve**: Set `first_party`, `third_party`, or `unknown` only if ownership cannot be established.

### mcp_servers[<N>].version
- **Current value**: not defined
- **Why it matters**: Needed for **ARCH-008** when the server is not first-party.
- **How to resolve**: Provide the pinned server version.

### mcp_servers[<N>].content_hash
- **Current value**: not defined
- **Why it matters**: Needed for **ARCH-008** when the server is not first-party.
- **How to resolve**: Provide a trusted checksum or digest.

### policies.escalation_rules
- **Current value**: not defined
- **Why it matters**: Enables **TOOL-001**, **TOOL-003**, and **TOOL-004**.
- **How to resolve**: Define when human approval or confirmation is required.

### policies.denied_actions
- **Current value**: not defined
- **Why it matters**: Strengthens **TOOL-001**, **TOOL-003**, and **TOOL-004**.
- **How to resolve**: List actions the agent must never perform.

### data_sources[<N>].sensitivity
- **Current value**: `unknown`
- **Why it matters**: Enables **TOOL-005** (high) and **TOOL-006** (critical).
- **How to resolve**: Choose `public`, `internal`, or `confidential`.

### has_persistent_memory
- **Current value**: `unknown`
- **Why it matters**: Enables **ARCH-004** and **ARCH-006**.
- **How to resolve**: Confirm whether state persists across sessions.

### scope
- **Current value**: `unknown`
- **Why it matters**: Enables **ARCH-005** and **ARCH-006**.
- **How to resolve**: Set `single_user`, `shared_workspace`, or `multi_tenant`.

## Medium Priority

### policies.allowed_actions
- **Current value**: not defined
- **Why it matters**: Enables **TOOL-002** (medium).
- **How to resolve**: List explicitly allowed tools.

### policies.max_tool_calls
- **Current value**: not defined
- **Why it matters**: Helps satisfy **ARCH-007** (medium).
- **How to resolve**: Set a maximum number of tool calls per task/session.

### policies.max_steps
- **Current value**: not defined
- **Why it matters**: Helps satisfy **ARCH-007** (medium).
- **How to resolve**: Set a maximum autonomous step count.

### policies.rate_limits
- **Current value**: not defined
- **Why it matters**: Helps satisfy **ARCH-007** (medium).
- **How to resolve**: Define call-rate ceilings such as `"10 calls/minute"`.

### policies.cost_budget
- **Current value**: not defined
- **Why it matters**: Helps satisfy **ARCH-007** (medium).
- **How to resolve**: Define a spend ceiling such as `"$1.00/session"`.

### tools[<N>].parameters.properties.<dangerous_param>
- **Current value**: unconstrained or unknown
- **Why it matters**: Enables more reliable **TOOL-007** analysis.
- **How to resolve**: Add schema constraints such as `enum`, `pattern`, `format`, `maxLength`, or closed objects.

### policies.data_boundaries
- **Current value**: does not mention tenant isolation or memory protections
- **Why it matters**: Improves **ARCH-004** / **ARCH-006** evidence quality.
- **How to resolve**: Add explicit memory-poisoning and tenant-isolation controls when applicable.

### user_input_description
- **Current value**: not defined
- **Why it matters**: Improves remediation quality.
- **How to resolve**: Describe the user input channel briefly.
```

### Notes

- Only include fields that are actually missing, `unknown`, or weakly evidenced
- Use actual indices such as `tools[0]`
- Reference current rule IDs only
- Prefer actual field paths over generic prose
